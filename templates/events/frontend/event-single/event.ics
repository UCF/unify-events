{% load bleach_tags %}
BEGIN:VEVENT
UID:http://{{ request.get_host }}{{ event_instance.get_absolute_url }}
DTSTAMP:{{ event_instance.start|date:"Ymd\THis" }}
DTSTART:{{ event_instance.start|date:"Ymd\THis" }}
DTEND:{{ event_instance.end|date:"Ymd\THis" }}
SUMMARY:{% if event_instance.event.canceled %}CANCELED - {% endif %}{{ event_instance.title|linebreaksbr }}
DESCRIPTION:{{ event_instance.event.description|bleach|linebreaksbr }}
END:VEVENT
