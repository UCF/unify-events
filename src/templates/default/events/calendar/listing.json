{
	'calendar'   : '{{calendar.slug}}',
	'start'      : "{{start|date:"r"}}",
	'end'        : "{{end|date:"r"}}",
	'event_count': {{events|length}},
	'events'     : [
	{%for event in events%}
		{%include 'events/calendar/parts/event.json'%}{%if not forloop.last%},{%endif%}
	{%endfor%}
	]
}